import { FC } from 'react';
import { Outlet } from 'react-router';

const ProfilePage: FC = () => {
    return (
        <>
            <div>Профиль</div>
        </>
    );
};

export default ProfilePage;
