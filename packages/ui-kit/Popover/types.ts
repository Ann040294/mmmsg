export enum PopoverPosition {
    LEFT = 'left',
    RIGHT = 'right',
    TOP = 'top',
    BOTTOM = 'bottom',
}

export enum PopoverSide {
    START = 'start',
    CENTER = 'center',
    END = 'end',
}
